#include "flatbuffers/flatbuffers.h"
#include <iostream>


namespace flatbufferMutator
{

    // we open abstry flat buffer file given the scheame
    // do we write to a flat buffer bin file then the test program reads it 

}

int main(int) 
{
    std::cout << "Flat buffer" << std::endl;
}